spring:
  jpa:
    hibernate:
      ddl-auto: none

  datasource:
    url: jdbc:mariadb://localhost:3306/myDB?useSSL=false&serverTimezone=Asia/Seoul&useUnicode=true
    username: root
    password: 1234
    driver-class-name: org.mariadb.jdbc.Driver
