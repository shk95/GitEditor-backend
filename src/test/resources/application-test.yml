spring:
  config:
    import: application-test_secret.yml

  jpa:
    hibernate:
      ddl-auto: none

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:gitEditor;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:

logging:
  level:
    org.springframework: DEBUG
